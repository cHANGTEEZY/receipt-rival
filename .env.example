# Base URL of your external Better Auth / API server (no trailing slash).
# Copy this file to `.env` / `.env.local` and adjust for your setup.
#
# Examples:
#   Local server (iOS simulator / web):  http://localhost:3000
#   Local server (Android emulator):     http://localhost:3000
#     (this app rewrites localhost → 10.0.2.2 on Android automatically)
#   Physical device:                     http://192.168.x.x:3000  (your machine's LAN IP)
EXPO_PUBLIC_API_URL=http://localhost:3000

# Restart Metro after changing env vars: npx expo start --clear
#
# On your Better Auth *server* (not this app), add trusted origins:
#   trustedOrigins: [
#     "expoboilerplate://",
#     "exp://",
#     "exp://**",
#   ]
